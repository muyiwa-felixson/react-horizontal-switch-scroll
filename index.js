/**
 * @file export classes here 
 */
const {
    SwitchCarousel,
    dataList,
    Wrapper,
    ListWrapper,
    List
} = require('dist/switchCarousel.components');
module.exports = {
    SwitchCarousel,
    dataList,
    Wrapper,
    ListWrapper,
    List
};